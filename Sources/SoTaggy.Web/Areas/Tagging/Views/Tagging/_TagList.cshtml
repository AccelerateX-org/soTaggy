@model SoTaggy.Web.Areas.Tagging.Models.ImageDisplayModel


<table class="table table-striped">
    <thead>
        <tr>
            <th style="width:66%">Schlagwort</th>
            <th style="width:33%">Option</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Tags)
        {
            <tr>
                <td>@item.Tag</td>
                <td>@Ajax.ActionLink("Löschen", "DeleteTag", "Tagging",
                    new { id = item.ArtworkTagId },
                    new AjaxOptions()
                    {
                        HttpMethod = "Post",
                        UpdateTargetId = "TagList",
                        InsertionMode = InsertionMode.Replace,
                        OnSuccess = "RemoveTag",
                    },
                    new
                    {
                        @class="btn btn-danger",
                    })</td>
            </tr>
        }
    </tbody>
</table>
